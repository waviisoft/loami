# API reference

The full API reference is generated by `rustdoc` and published alongside this guide:

👉 **<https://waviisoft.github.io/loami/api/>**

It is rebuilt and redeployed on every push to `main`. To browse it locally:

```sh
cargo doc --no-deps --open
```
