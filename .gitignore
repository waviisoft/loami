# Rust
/target/
**/*.rs.bk
*.pdb
# Cargo.lock is committed: this workspace ships binaries (CLI) and we want reproducible CI builds.

# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo
.npm/
.pnp.*
.yarn/*

# napi-rs build artifacts
*.node

# mdBook build output
docs/book/

# Environment / secrets
.env
.env.*
!.env.example

# Loami local data backends (file:// store, caches)
*.loami
/.loami/
/data/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
