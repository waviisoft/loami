<!--
Summary (required) — replace this comment with 1–3 prose paragraphs, with NO
heading above them, describing the problem and the change. This text becomes
the squash commit body verbatim, so it must be self-contained: do not
reference code symbols, other PRs, issues, or commits here. References to
other repositories are allowed.

Keep the whole description up to date as new commits land on the branch — it
always reflects the current state of the change, not just its first version.
-->

## Why

<!-- Optional — motivation and background. References to code symbols,
issues, PRs, and commits are allowed here. Delete the section if the Summary
already covers it. -->

## Changes

<!-- The files changed and what changed in each. -->

- `path/to/file` — what changed and why it changed here

## Test Plan

<!-- Optional — only extra testing needed BEYOND what CI does. Delete the
section if CI fully covers the change. -->

## Issues

<!-- Optional — issues this PR resolves or fixes, e.g. "Fixes #12". Delete
the section if none. -->
